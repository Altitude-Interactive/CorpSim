---
type: patch
area: web
summary: Add item search, filters, and page-size pagination controls to the development catalog.
---

- Add item search by code/name in the dev catalog.
- Add item filters for source (`base` vs `icon`) and tier (`1-4`).
- Limit item table pagination to page sizes of `10`, `20`, `50`, or `100`.
