---
type: minor
area: api, web, auth
summary: Add Google OAuth authentication with Better Auth and env-gated sign-in UI
---

- Add Better Auth Google provider configuration in API using env-based credentials.
- Add Google sign-in/sign-up actions on web auth pages behind a public feature flag.
- Add Google auth environment variables to `.env.example` for local and deployment setup.
